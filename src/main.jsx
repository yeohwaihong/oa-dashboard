import React from "react";
import ReactDOM from "react-dom/client";
import OABookingDashboard from "../oa_booking_dashboard_frontend.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OABookingDashboard />
  </React.StrictMode>,
);
